import { createSlice } from '@reduxjs/toolkit';\n\n// Generate dummy data\nconst createDummyData = (count = 50) => {\n  const rows = [];\n  for (let i = 1; i <= count; i++) {\n    rows.push({\n      id: i,\n      firstName: `First${i}`,\n      lastName: `Last${i}`,\n      email: `user${i}@example.com`,\n      age: 20 + (i % 30),\n      city: ['New York','London','Tokyo','Berlin','Mumbai'][i % 5]\n    });\n  }\n  return rows;\n};\n\nconst initialState = {\n  data: createDummyData(50),\n};\n\nconst tableSlice = createSlice({\n  name: 'table',\n  initialState,\n  reducers: {\n    addRow: (state, action) => {\n      const nextId = state.data.length ? (Math.max(...state.data.map(d => d.id)) + 1) : 1;\n      state.data.push({ id: nextId, ...action.payload });\n    },\n  },\n});\n\nexport const { addRow } = tableSlice.actions;\nexport default tableSlice.reducer;